// Copy this file to config.ts and replace with your local PC's IPv4 address
// Find it with: ipconfig (Windows) or ifconfig (macOS/Linux)
// Do NOT use 'localhost' — on a physical device that refers to the phone itself
export const BACKEND_URL = "http://YOUR_PC_IP:8000";
